👾

Список причин почему тесты 
валятся на платформе, но 
локально проходят.

Ниже представлен чек-лист основных проблем, из-за которых проект может 

не проходить тесты на платформе, но при этом проходить тесты локально. 
Студенту необходимо пройтись по нему и проверить нет ли у него ошибок, 
которые приведены в чек-листе:

Проблемы общего характера

1.  Локально тесты проходят, а на платформе нет.  

— Проверьте, что у вас стоит python 3.7 (на серверах нет версии 3.8). 
— Проверьте, что тесты не изменены локально. Бывает, что тесты на 

платформе были обновлены, а у студента они остались прошлой версии.  
Если тесты отличаются от актуальных, можете спулить их из исходного 

репозитория с проектом от Практикума: 
git pull https://github.com/yandex-praktikum/НАЗВАНИЕ_РЕПОЗИТОРИЯ.git 
— Проверьте, что в .gitignore нет никаких файлов, которые влияют на запуск 
проекта (например конфигов). 

— Убедитесь, что все необходимые файлы были включены в коммит и попали 

в репозиторий.  
— Проверьте, что не переименовывали константы из исходного репозитория. 
Они используются при тестировании на платформе. Например:

PRACTICUM_TOKEN = os.getenv("PRACTICUM_TOKEN")

Список причин почему тесты валятся на платформе, но локально проходят.1 — Для констант, которые берутся из переменных окружения, рекомендуется 

указывать значения по умолчанию (они будут использоваться на платформе в 

тестах). Обратите внимание, значения по умолчанию должны быть 

валидными, и проходить тесты в случае отсутствия .env файла!

Например:

SECRET_KEY = os.getenv("SECRET_KEY", default="SUP3R-S3CR3T-K3Y-F0R-MY-PR0J3CT")

— Проверьте регистр названий папок и файлов (git может не увидеть 

изменения регистра имени файла/папок). Команда  git config core.ignorecase 

false  сделает git чувствительным к регистру. 
(https://stackoverflow.com/questions/52950/how-to-make-git-ignore-changes-in-
case)

2.  Проект не загружается на платформу. 

- Переименован репозиторий, или актуальная версия проекта находится в 
ветке отличной от master, например main.  
- Если проект загружался архивом, он может не соответствовать формату 
github. Платформа поддерживает только формат .zip определенной степени 

сжатия. Во избежании проблем, следует загружать архивы, созданные из 
репозитория github по кнопке (см. скриншот ниже).

Список причин почему тесты валятся на платформе, но локально проходят.2Создаваемые на macOS архивы часто имеют другую степень сжатия, и 

вызывают ошибки при отправке на платформу. 

Проблемы Django-приложений

1.  Локально тесты проходят, а на платформе нет.  

— В проекте используются модули, которых нет в зависимостях. Необходимо 

привести в порядок зависимости или не использовать дополнительные 

модули. (ЭТО САМЫЙ ЧАСТЫЙ СЛУЧАЙ). 

— Приложение не добавлено в  INSTALLED_APPS  в  settings.py   

— Приложения в  INSTALLED_APPS  надо подключать после стандартных, а не до. 
— Отсутствуют миграции в репозитории. 
     Для того, чтобы создать миграции: 
     python3  manage.py  makemigrations 

— В settings.py  - пути вида  BASE_DIR / 'static'  могут вызывать ошибку в 
последних версиях Django в сочетании с Python ≤ 3.7:  TypeError: argument of type 
'PosixPath' is not iterable  
Варианты решения:  

str(BASE_DIR / 'static')

os.path.join(BASE_DIR, 'static'),

Список причин почему тесты валятся на платформе, но локально проходят.3